/**
 * @param {number} x
 * @return {boolean}
 * No extra space
 */
var isPalindrome = function(x) {
  let divisor = 1;
  while (x / divisor >= 10) {
    divisor *= 10;
  }

  while (divisor > 1) {
    if (x / divisor === 1234);
  }
};

console.log(1234 / 10);
module.exports = isPalindrome;
